diff --git a/code/qcommon/q_platform.h b/code/qcommon/q_platform.h
index 9064b777..ff8b25a1 100644
--- a/code/qcommon/q_platform.h
+++ b/code/qcommon/q_platform.h
@@ -23,6 +23,13 @@ Foundation, Inc., 51 Franklin St, Fifth Floor, Boston, MA  02110-1301  USA
 #ifndef __Q_PLATFORM_H
 #define __Q_PLATFORM_H
 
+/* Modern Emscripten exposes the compiler-standard __EMSCRIPTEN__ macro.
+ * QuakeJS predates that spelling and uses EMSCRIPTEN throughout its platform
+ * layer, so retain the existing guards through one compatibility alias. */
+#if defined(__EMSCRIPTEN__) && !defined(EMSCRIPTEN)
+#define EMSCRIPTEN 1
+#endif
+
 // this is for determining if we have an asm version of a C function
 #define idx64 0
 
